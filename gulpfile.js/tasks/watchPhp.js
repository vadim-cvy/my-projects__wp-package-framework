const gulp = require('gulp');

// const browserSync = require('./../browserSync');

gulp.task( 'watchPhp', () =>
{
    // gulp.watch([
    //     '**/*.php',
    //     '**/*.html',
    //     '**/*.txt',
    //     '**/*.json',
    // ]).on( 'change', browserSync.reload );
});